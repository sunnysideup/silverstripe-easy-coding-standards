#!/bin/bash
SCRIPT_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" >/dev/null 2>&1 && pwd )"
WORKING_DIR=$(pwd)
: ${dir:=app/src}
: ${level:=4}
cp vendor/sunnysideup/easy-coding-standards/phpstan.neon ./
vendor/bin/phpstan analyse -l $level -c phpstan.neon $dir
